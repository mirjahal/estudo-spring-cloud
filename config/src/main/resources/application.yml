server:
  port: 9090

spring:
  cloud:
    config:
      server:
        git:
          uri: /home/almir/Pessoal/Projects/Java/spring-cloud/config-files
    bus:
      enabled: true

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
